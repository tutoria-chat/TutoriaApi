# Force HTTPS redirect for production
# Redirects HTTP requests to HTTPS when behind load balancer

server {
    listen 8080;

    if ($http_x_forwarded_proto = 'http') {
        return 301 https://$host$request_uri;
    }
}

# Increase nginx client max body size to allow file uploads up to 50MB
# This prevents "client intended to send too large body" errors
# Set to 50MB to accommodate file size + HTTP headers + multipart overhead

client_max_body_size 50M;
